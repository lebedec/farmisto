use crate::stockraising::{Stockraising, StockraisingDomain};

impl StockraisingDomain {
    pub fn update(&mut self, time: f32) -> Vec<Stockraising> {
        let mut events = vec![];
        for animal in self.livestocks.iter_mut() {}
        events
    }
}
