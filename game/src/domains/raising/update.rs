use crate::raising::{Raising, RaisingDomain};

impl RaisingDomain {
    pub fn update(&mut self, time: f32) -> Vec<Raising> {
        let mut events = vec![];
        for animal in self.animals.iter_mut() {}
        events
    }
}
